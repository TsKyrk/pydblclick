import pydblclick  # optional: pip install pydblclick -- or delete this line
from tkinter import *

root = Tk()
a = Label(root, text="Hello World")
a.pack()
root.mainloop()
# A .pyw script: no console at all, just the window.
